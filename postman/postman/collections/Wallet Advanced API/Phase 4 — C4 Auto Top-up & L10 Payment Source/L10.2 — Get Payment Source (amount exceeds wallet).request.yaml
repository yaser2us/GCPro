$kind: http-request
description: "L10: Split payment scenario — wallet covers partial amount,
  remainder via external payment intent."
url: "{{base_url}}/api/v1/wallet/payment-source?account_id={{account_id}}&amoun\
  t=9999&currency=MYR"
method: GET
headers:
  Authorization: Bearer {{token}}
queryParams:
  account_id: "{{account_id}}"
  amount: "9999"
  currency: MYR
order: 2000
