$kind: http-request
description: "Publish a draft version. Version status: draft → published. Emits
  GUIDELINE_VERSION_PUBLISHED. Only published versions can be accepted."
url: "{{base_url}}/api/v1/foundation/guideline-versions/{{version_id}}/publish"
method: POST
headers:
  Content-Type: application/json
  x-user-id: "{{admin_user_id}}"
  x-user-role: "{{admin_user_role}}"
body:
  type: text
  content: "{}"
scripts:
  - type: beforeRequest
    code: "pm.request.headers.add({ key: 'Idempotency-Key', value:
      pm.variables.replaceIn('{{$guid}}') });"
    language: text/javascript
  - type: afterResponse
    code: pm.test('Status is 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
