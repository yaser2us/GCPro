$kind: http-request
url: "{{base_url}}/v1/surveys/{{survey_id}}/deactivate"
method: POST
headers:
  Idempotency-Key: deactivate-survey-{{$randomUUID}}
  X-User-Id: "{{user_id}}"
  X-User-Role: ADMIN
body:
  type: json
  content: ""
order: 4000
