$kind: http-request
description: |-
  Pause an active referral program

  Spec: specs/referral/referral.pillar.yml lines 955-989
  Permissions: referral:admin
url: "{{base_url}}/v1/referral/programs/{{program_id}}/pause"
method: POST
headers:
  Content-Type: application/json
  Idempotency-Key: "{{$randomUUID}}"
  X-User-Id: "{{actor_user_id}}"
  X-User-Role: ADMIN
body:
  type: text
  content: |-
    {
      "reason": "Program needs maintenance"
    }
examples: ./.resources/Pause Referral Program.resources/examples
order: 2000
