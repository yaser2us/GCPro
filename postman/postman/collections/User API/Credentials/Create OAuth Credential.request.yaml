$kind: http-request
url: "{{base_url}}/v1/users/{{user_id}}/credentials"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "type": "oauth",
      "provider": "google",
      "provider_user_id": "google-user-12345"
    }
order: 2000
