$kind: http-request
description: Reset password credential
url: "{{base_url}}/v1/user-credentials/{{credential_id}}/reset"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "new_secret": "NewSecureP@ssw0rd456"
    }
order: 4000
