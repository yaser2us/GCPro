$kind: http-request
description: |-
  Assign role to user

  Idempotency: Via UNIQUE(user_id, role_id)
url: "{{base_url}}/v1/users/{{user_id}}/roles"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "role_id": "{{role_id}}"
    }
order: 1000
