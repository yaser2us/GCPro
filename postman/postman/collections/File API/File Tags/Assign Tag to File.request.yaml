$kind: http-request
description: |-
  Assign tag to file

  Idempotency: Via UNIQUE(file_id, tag_id)
url: "{{base_url}}/v1/files/{{file_id}}/tags"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "tag_id": "{{tag_id}}"
    }
order: 3000
