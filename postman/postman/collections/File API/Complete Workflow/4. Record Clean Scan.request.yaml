$kind: http-request
url: "{{base_url}}/v1/files/{{workflow_file_id}}/scan-results"
method: POST
headers:
  Content-Type: application/json
  Idempotency-Key: "{{$guid}}"
body:
  type: text
  content: |-
    {
      "scan_type": "antivirus",
      "status": "clean",
      "provider": "ClamAV"
    }
order: 4000
