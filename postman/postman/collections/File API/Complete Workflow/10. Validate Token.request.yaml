$kind: http-request
url: "{{base_url}}/v1/file-tokens/validate"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "token": "{{workflow_token}}",
      "increment_usage": true
    }
order: 10000
