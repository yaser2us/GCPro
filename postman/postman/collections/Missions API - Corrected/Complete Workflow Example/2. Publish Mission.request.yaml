$kind: http-request
url: "{{baseUrl}}/v1/missions/definitions/{{mission_definition_id}}/publish"
method: POST
headers:
  Content-Type: application/json
  Idempotency-Key: publish-mission-{{$timestamp}}
  X-User-Id: "{{actor_user_id}}"
  X-User-Role: ADMIN
body:
  type: text
  content: "{}"
order: 2000
