$kind: http-request
description: Get reward grant for a completed assignment. Requires missions:read
  or missions:admin permission.
url: "{{baseUrl}}/v1/missions/assignments/{{assignment_id}}/reward-grant"
method: GET
headers:
  X-User-Id: "{{user_id}}"
  X-User-Role: USER
order: 5000
