$kind: http-request
description: Get all missions assigned to a user. Requires missions:read,
  missions:admin, or missions:enroll permission.
url: "{{baseUrl}}/v1/users/{{user_id}}/mission-assignments"
method: GET
headers:
  X-User-Id: "{{user_id}}"
  X-User-Role: USER
order: 3000
