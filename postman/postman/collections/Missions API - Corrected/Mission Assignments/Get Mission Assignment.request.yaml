$kind: http-request
description: Get assignment details by ID. Requires missions:read,
  missions:admin, or missions:enroll permission.
url: "{{baseUrl}}/v1/missions/assignments/{{assignment_id}}"
method: GET
headers:
  X-User-Id: "{{user_id}}"
  X-User-Role: USER
order: 2000
